---
# =============================================================================
# Playbook  : cisco_reachability_check.yml
# Purpose   : Check reachability of all Cisco switches in the inventory
# Platform  : Ansible AWX
# Author    : Network Engineering Team
# Date      : 2026-04-16
# =============================================================================
# Requirements:
#   - Collection: cisco.ios (ansible-galaxy collection install cisco.ios)
#   - Collection: ansible.netcommon
#   - AWX Credential type: Network (SSH or username/password)
#
#     vars:
#       ansible_network_os: cisco.ios.ios
#       ansible_connection: ansible.netcommon.network_cli
#       ansible_user: "{{ vault_switch_user }}"
#       ansible_password: "{{ vault_switch_password }}"
#       ansible_become: true
#       ansible_become_method: enable
#       ansible_become_password: "{{ vault_enable_password }}"
# =============================================================================

- name: Check reachability of all Cisco switches
  hosts: all
  gather_facts: false
  ansible_connection: ansible.netcommon.network_cli
  ignore_unreachable: true   # Do NOT mark task as failed if host is unreachable

  tasks:

    - name: Send 'show clock' command to verify connectivity
      cisco.ios.ios_command:
        commands:
          - show clock
      register: probe_result
      ignore_errors: true

    - name: Set host status to REACHABLE
      ansible.builtin.set_fact:
        host_status: "REACHABLE"
        host_output: "{{ probe_result.stdout[0] | default('') }}"
      when:
        - probe_result is not unreachable
        - probe_result is not failed

    - name: Set host status to UNREACHABLE
      ansible.builtin.set_fact:
        host_status: "UNREACHABLE"
        host_output: "{{ probe_result.msg | default('Connection failed') }}"
      when: >
        probe_result is unreachable or
        probe_result is failed

# -----------------------------------------------------------------------------
# Generate consolidated report on the AWX controller (localhost)
# -----------------------------------------------------------------------------
- name: Generate reachability report
  hosts: localhost
  gather_facts: false

  tasks:

    - name: Build reachable hosts list
      ansible.builtin.set_fact:
        reachable_hosts: >-
          {{
            groups['all']
            | map('extract', hostvars)
            | selectattr('host_status', 'defined')
            | selectattr('host_status', 'equalto', 'REACHABLE')
            | map(attribute='inventory_hostname')
            | list
          }}

    - name: Build unreachable hosts list
      ansible.builtin.set_fact:
        unreachable_hosts: >-
          {{
            groups['all']
            | map('extract', hostvars)
            | selectattr('host_status', 'defined')
            | selectattr('host_status', 'equalto', 'UNREACHABLE')
            | list
            | map(attribute='inventory_hostname')
            | list
          }}

    - name: Per-host status detail
      ansible.builtin.debug:
        msg: "[{{ hostvars[item].host_status | default('UNKNOWN') }}] {{ item }} ({{ hostvars[item].ansible_host | default('no IP') }}) | {{ hostvars[item].host_output | default('no output') }}"
      loop: "{{ groups['all'] }}"
      loop_control:
        label: "{{ item }}"

    - name: "==== REACHABILITY SUMMARY ===="
      ansible.builtin.debug:
        msg:
          - "Total switches checked : {{ groups['all'] | length }}"
          - "Reachable              : {{ reachable_hosts | length }} -> {{ reachable_hosts }}"
          - "Unreachable            : {{ unreachable_hosts | length }} -> {{ unreachable_hosts }}"

    - name: Fail the playbook if any switch is unreachable
      ansible.builtin.fail:
        msg: "One or more switches are UNREACHABLE: {{ unreachable_hosts }}"
      when: unreachable_hosts | length > 0

